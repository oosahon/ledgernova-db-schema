export const ledgerAccountsTable = { schema: 'core', name: 'ledger_accounts' };

export const ledgerType = { schema: 'core', name: 'ledger_type' };

export const normalBalanceType = {
  schema: 'core',
  name: 'normal_balance_type',
};

export const ledgerAccountStatus = {
  schema: 'core',
  name: 'ledger_account_status',
};

export const contraAccountRule = {
  schema: 'core',
  name: 'contra_account_rule',
};

export const adjunctAccountRule = {
  schema: 'core',
  name: 'adjunct_account_rule',
};
